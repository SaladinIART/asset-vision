# Minimal setup.py stub — required for `pip install -e .` on pip < 21.3
# (older system Python3 / Ubuntu 22.04 system pip does not support PEP 660
# editable installs from pyproject.toml alone).
# All real metadata lives in pyproject.toml.
from setuptools import setup

setup()
